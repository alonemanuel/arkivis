/**
 * User-related type definitions for Arkivis
 * Contains shared interfaces used across components and pages
 */

export interface UserInfo {
  name: string;
  email: string;
  picture?: string;
} 